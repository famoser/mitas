{% block era_entry_widget %}
    <div class="row">
        <div class="col-md-6">
            {{ form_row(form.fullName) }}
        </div>
        <div class="col-md-6">
            {{ form_row(form.email) }}
        </div>
    </div>
    <div class="row">
        <div class="col-md-3">
            {{ form_row(form.workMode) }}
        </div>
        <div class="col-md-9">
            {{ form_row(form.team) }}
        </div>
    </div>
{% endblock %}
